#include <stdio.h>

int main() {
    int num;
    
    printf("enter num: ");
    scanf("%d", &num);


    if (num % 2 == 0) {
       printf("the number entered is not an odd no.");

}

    else {
	printf("the number entered is an odd no.");
}

